{% if url != blank %}
  <a class="custom-footer__social-link" href="{{ url }}" aria-label="{{ label | escape }}">
    <img src="{{ icon | asset_url }}" width="24" height="24" alt="">
  </a>
{% endif %}
